<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.3.0/font/bootstrap-icons.css">
</head>
<body>
    <div class="navContainer container-fluid bg-dark" >
    <nav class="navbar navbar-dark bg-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="#">
            SoloWhisper
          </a>
        </div>
      </nav>
      <a class="btn text-white" href="/logout">Logout</a>
      </div> 
      <div class="newPost">
      <a href="/new" class="btn btn-outline-info mt-3 h-25 w-25 create">Create <i class="bi bi-pencil"></i></a>
      </div>
    <div class="Contents">
        <% post.forEach(post=>{%>
            <div class="contentContainer border border-light-subtle rounded shadow-sm">
                <div class="spaceAdd">
                    <div>
                    <h3> <%= post.title%></h3>
                    <div class="text-muted">
                        <%= post.createdAt.toLocaleDateString() %>
                    </div>
                    <div>
                       <p> <%=post.description %></p>
                    </div>
                    </div>
                    <div class="link">
                    <a href="/<%= post.id %>">Read more.....</a>
                    </div>
                </div>
            </div>
        <%})%>
    </div>
</body>
</html>